from setuptools import setup

setup(name='network',
      version='1.0',
      description="Network package",
      long_description="",
      author='Angus Hollands',
      author_email='goosey15@gmail.com',
      license='MIT',
      packages=['network'],
      zip_safe=False,
      install_requires=[
          # 'Sphinx',
          # ^^^ Not sure if this is needed on readthedocs.org
          # 'something else?',
          ],
      )
setup(name='bge_network',
      version='1.0',
      description="BGE network package",
      long_description="",
      author='Angus Hollands',
      author_email='goosey15@gmail.com',
      license='MIT',
      packages=['bge_network'],
      zip_safe=False,
      install_requires=[
          # 'Sphinx',
          # ^^^ Not sure if this is needed on readthedocs.org
          # 'something else?',
          ],
      )
setup(name='blender_example',
      version='1.0',
      description="Blender Example package",
      long_description="",
      author='Angus Hollands',
      author_email='goosey15@gmail.com',
      license='TODO',
      packages=['blender_example'],
      zip_safe=False,
      install_requires=[
          # 'Sphinx',
          # ^^^ Not sure if this is needed on readthedocs.org
          # 'something else?',
          ],
      )
setup(name='game_system',
      version='1.0',
      description="Game System package",
      long_description="",
      author='Angus Hollands',
      author_email='goosey15@gmail.com',
      license='TODO',
      packages=['game_system'],
      zip_safe=False,
      install_requires=[
          # 'Sphinx',
          # ^^^ Not sure if this is needed on readthedocs.org
          # 'something else?',
          ],
      )